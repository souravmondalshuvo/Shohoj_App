#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.debug.xcconfig"
#include "Generated.xcconfig"

// Optional per-machine overrides. Not in version control — see
// ios/Flutter/Local.xcconfig.example and the README. Included last so it wins.
#include? "Local.xcconfig"
